อย่างแรกเลยที่เห็นโจทย์เป็นตาราง grid น่าจะต้องใช้ 2D array แบบเป็น array ซ้อน array เพราะไปสืบค้นมาแล้ว ไม่มีแบบ 2D array เหมือน numpy ใน python ในการสร้างตาราง grid ต้องกำหนด row กับ column และมีการกำหนด percentage of hole โดยในการเริ่ม format แรก น่าจะต้อง fill 2D array นี้ให้เต็มก่อนด้วย fieldcharacter จากนั้นจะเริ่มการสุ่มการใส่ตัว hat และ character ตามลำดับ แต่ปัญหาในการสุ่มที่อาจจะเจอคือ สุ่มซ้ำซ้อนกัน ทำให้ทับตำแหน่งเดียวกัน เลยแก้ปัญหา ให้เติม hole ที่เยอะที่สุดก่อนออกไป ให้เติมครบตามจำนวนเปอร์เซ็นต์ที่ตั้งไว้ให้เต็ม โดยที่มีการวนลูป หากช่องที่สุ่มเป็นช่องเดิม จากนั้นค่อยเติม hat และ character ให้เต็ม โดยการวนลูปเช็คว่าเป็นที่ว่าง และสุ่มซ้ำซ้อนหรือไม่ จากนั้นเมื่อลองเขียน printMap และลองรันแล้ว ดันพบเจอปัญหาว่า การสุ่มโดยที่ไม่มีเงื่อนไขนั้น ทำให้เกิดหลุมร้อนตัวละคร หรือหลุมล้อม hat หรือไม่มีทางที่ตัวละคร จะเดินไปหา hat ได้ เลยพยายามแก้ปัญหาสร้าง method isSolvable ขึ้นมาเพื่อเช็คเงื่อนไขว่าสามารถเดินไปได้หรือไม่ โดยการกำหนดจุดเริ่มต้น และทดลองเดินทั้งบน ล่าง ซ้าย ขวาแบบสุ่ม แล้วเก็บ history การเดินทุก path  เก็บเป็น stack เอาค่าล่าสุดออกก่อน แล้วค่อยถอยมาเรื่อยๆ คล้ายกับการทำ depth first search เพื่อให้มี path ในการเดินไปจนถึง hat ให้ได้ ถึงจะ return true เหมือนการแก้ปัญหา Maze runner เมื่อทดสอบแล้ว พบว่า ไม่เสียเวลามากเกินไป ในการทดลองทำทุก path หากจำนวน row, col ไม่ได้เยอะมากเกินไป จากนั้นค่อยถึง step ของการเดิน แต่จะต้องมีการใช้ position ล่าสุด เลยเขียน method หา position ของ character แล้วเรียกใช้ ในการหาค่าเริ่มต้น จากนั้นกำหนดแต่ละทิศ โดยการคำนวณ row หรือ column ขึ้นมาใหม่ และหาค่าว่าเป็นค่าอะไร hole หรือ hat หรืออกนอกขอบเขตตามแต่ละเงื่อนไข ให้ return เป็นค่า boolean เพื่อใช้งานใน loop ในการกำหนดการออกจาก loop ก่อน ไม่งั้นจะออกจาก loop  ไม่ได้ จากนั้นสุดท้าย เขียน running โดยการวนลูปซ้ำๆ ให้ print และรับค่าคำสั่ง จากนั้นกำหนดเงื่อนไขที่จะทำ โดยการเรียกใช้แต่ละ method บนล่างซ้ายขวา และมีการกำหนดให้ออกจากลูป ตามเงื่อนไขที่กำหนด